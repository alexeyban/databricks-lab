.env
__pycache__/
.DS_Store
*.pyc
.checkpoints
.databrickscfg
venv/
logs/
scripts/dvdrental.sql
infra/docker/data/**

# Archived generator (static dv_model.json used instead)
archive/

# pump.fun ingestor — local JSONL landing buffer and its venv
ingestion/pumpfun/data/
ingestion/pumpfun/venv/

# Vendored dbt package docs (the project's own docs/ are tracked)
transformation/dbt_project/dbt_packages/**/docs/
